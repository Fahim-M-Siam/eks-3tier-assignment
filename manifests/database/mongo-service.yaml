apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: three-tier
  labels:
    app: mongodb
    tier: database
spec:
  selector:
    app: mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP
